# Minimal rootfs for npm ecosystem sandbox validation
FROM node:20-alpine AS base
RUN apk add --no-cache ca-certificates && \
    rm -rf /var/cache/apk/* /tmp/* /root/.cache && \
    adduser -D -u 99999 sandbox

FROM scratch
COPY --from=base / /
USER 99999
WORKDIR /tmp
